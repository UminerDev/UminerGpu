# Uminer GPU v0.1.2

## 修复

- 使用 BTX 矿池下发的 \`nonce64_start\` 作为扫描起点，确保提交的 nonce 落在矿池为连接分配的范围内。
- 将默认半死连接检测时间调整为 3600 秒，避免低消息频率的 BTX Stratum 连接被误判断开。

## 验证

- Ubuntu 24.04 / CUDA 13 / RTX 4090。
- \`global.btxpool.org:23333\` 实际 accepted share：\`A1 R0 S0\`。

已知遗留：Linux 产物仍要求 \`GLIBC_2.39\`。
